import type { Meta, StoryObj } from '@storybook/react';
import index from './index.tsx'

const meta: Meta<typeof index> = {
    component: index,
  };

export default meta
type Story = StoryObj<typeof index>

export const FistTest:Story = {
    
}